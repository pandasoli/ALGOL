BEGIN

  PROC write line = (STRING text) VOID: (
    print( text )
  );

  PROC sum = (REAL a, b) REAL:
  BEGIN
    a + b
  END;

  PROC say hi mom = VOID: print("hi");

  print((
    sum(2, 5),
    new line
  ));

  write line( "Hello, World!" );
  say hi mom
END